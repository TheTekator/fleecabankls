{{ form_start(monFormulaire) }}

<div class="form-group row">
    {{ form_label(monFormulaire.dateSouscription, "Date de souscription :", {'label_attr': {'class': 'col-sm-2 col-form-label'}}) }}
    {{ form_errors(monFormulaire.dateSouscription) }}
    <div class="col-sm-3">
        {{ form_widget(monFormulaire.dateSouscription, {'attr': {'class': 'form-control'}}) }}
    </div>
    {{ form_label(monFormulaire.finished, "Terminé ? :", {'label_attr': {'class': 'col-sm-1 col-form-label'}}) }}
    {{ form_errors(monFormulaire.finished) }}
    <div class="col-sm-2 input-group">
        {{ form_widget(monFormulaire.finished, {'attr': {'class': 'form-control'}}) }}
    </div>
</div>

<div class="form-group row">
    {{ form_label(monFormulaire.montant, "Montant déposé :", {'label_attr': {'class': 'col-sm-2 col-form-label'}}) }}
    {{ form_errors(monFormulaire.montant) }}
    <div class="col-sm-3 input-group">
        {{ form_widget(monFormulaire.montant, {'attr': {'class': 'form-control', 'style': 'text-align: right'}}) }}
        <div class="input-group-append">
            <span class="input-group-text">$</span>
        </div>
    </div>
    {{ form_label(monFormulaire.interets, "Interêt :", {'label_attr': {'class': 'col-sm-1 col-form-label'}}) }}
    {{ form_errors(monFormulaire.interets) }}
    <div class="col-sm-1 input-group">
        {{ form_widget(monFormulaire.interets, {'attr': {'class': 'form-control', 'style': 'text-align: right'}}) }}
        <div class="input-group-append">
            <span class="input-group-text">%</span>
        </div>
    </div>
</div>

{{ form_rest(monFormulaire) }}
{{ form_errors(monFormulaire) }}

<br/>
<div class="form-group row">
    <div class="offset-sm-2 col-sm-6">
        <input type="submit" class="btn btn-primary form-control" value="Valider">
    </div>
</div>

{{ form_end(monFormulaire) }}
