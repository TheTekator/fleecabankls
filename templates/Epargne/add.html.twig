{% extends 'base.html.twig' %}

{% block content %}
    <h1>Ajouter une épargne</h1>

    {{ include('Epargne/form.html.twig') }}

    <div>
        <a href="{{ path('show_client', {'id': monFormulaire.vars.value.client.id}) }}">Retourner à la page client</a>
    </div>
{% endblock %}